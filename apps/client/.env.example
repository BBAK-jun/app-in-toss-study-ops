# StudyOps 클라이언트 환경변수
# VITE_API_BASE_URL: API 서버 베이스 URL.
#
# 로컬 개발 (기본): 비워두면 Vite proxy(같은 출처 /auth, /studies, /rounds → 8787)를 사용한다.
#   VITE_API_BASE_URL=
# 원격 dev 서버 직접 호출:
#   VITE_API_BASE_URL=https://studyops-server.<your-subdomain>.workers.dev
VITE_API_BASE_URL=
